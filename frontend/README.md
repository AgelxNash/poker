# Frontend (Vite + React + TS)

Простой UI для Poker Planning. Подключается к backend REST и WS.

## Запуск
```bash
cd frontend
npm i
npm run dev
# VITE_API_URL (по умолчанию http://localhost:8080)
# VITE_WS_URL  (по умолчанию ws://localhost:7071)
```
