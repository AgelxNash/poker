# .env examples

Добавлены примеры конфигов окружения:
- `backend/.env.example` — порт, debug.
- `ws/.env.example` — порт WS.
- `frontend/.env.example` — Vite-URL’ы для API/WS.

Скрипт для быстрого копирования:
```bash
bash scripts/env-copy.sh
```
